ลงสีสี่เหลี่ยมทั้งจอ
g2d.fillRect(0, 0, getWidth(), getHeight());

วาดเส้นตรง
g2d.draw(new Line2D.Double(_, _, _, _));

วาดโค้ง
g2d.draw(new QuadCurve2D.Double(x, y, x, y, x, y));

วาดวงรี
g2d.draw(new Ellipse2D.Double(x, y, _, _));

หมุน
g2d.rotate(Math.toRadians(-20), x, y);        g2d.fill(new __.Double(x, y, _, _));
g2d.rotate(Math.toRadians(20), x, y);

ลงสีภายในรูปทรง
g2d.fill(new __.Double(x, y, _, _));